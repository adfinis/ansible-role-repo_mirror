---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance
    image: quay.io/debian:bullseye
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
